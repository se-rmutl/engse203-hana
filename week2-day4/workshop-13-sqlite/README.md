# 🗄️ Workshop 13: SQLite Basics

**ระยะเวลา:** 90 นาที (09:00-10:30)  
**ระดับ:** เริ่มต้น

## 🎯 วัตถุประสงค์

หลังจากทำ workshop นี้เสร็จ นักศึกษาจะสามารถ:
1. **เข้าใจ** ว่า database คืออะไรและทำไมต้องใช้
2. **เข้าใจ** พื้นฐาน SQL (SELECT, INSERT, UPDATE, DELETE)
3. **ใช้งาน** SQLite สร้าง database ง่ายๆ
4. **สร้าง** tables และเก็บข้อมูล
5. **ดึงข้อมูล** ออกมาใช้งาน
6. **เข้าใจ** primary key และ foreign key พื้นฐาน

---

## 📚 Database คืออะไร?

### ก่อนมี Database

สมมติเราทำ Todo App แบบง่ายๆ เราอาจเก็บข้อมูลแบบนี้:

```javascript
// เก็บใน array
const todos = [
  { id: 1, task: 'ซื้อของ', done: false },
  { id: 2, task: 'ทำการบ้าน', done: true }
];

// เก็บใน file
fs.writeFileSync('todos.json', JSON.stringify(todos));
```

**ปัญหาคือ:**
- ❌ ข้อมูลหายเมื่อปิดโปรแกรม
- ❌ ค้นหาข้อมูลช้า (ต้อง loop ทั้งหมด)
- ❌ หลายคนใช้พร้อมกันไม่ได้
- ❌ ไม่มีการจัดระเบียบข้อมูล
- ❌ ไม่มีการตรวจสอบความถูกต้อง

### หลังมี Database

```sql
-- เก็บใน database
CREATE TABLE todos (
  id INTEGER PRIMARY KEY,
  task TEXT NOT NULL,
  done BOOLEAN DEFAULT 0
);

-- ค้นหาง่าย
SELECT * FROM todos WHERE done = 0;

-- เพิ่มข้อมูล
INSERT INTO todos (task) VALUES ('ซื้อของ');
```

**ข้อดี:**
- ✅ ข้อมูลถูกเก็บถาวร
- ✅ ค้นหาเร็ว
- ✅ มีการจัดระเบียบ
- ✅ ตรวจสอบความถูกต้อง
- ✅ รองรับหลายคนใช้งาน

---

## 💡 Database = ตู้เก็บเอกสาร

ลองคิดว่า Database เหมือนตู้เก็บเอกสาร:

```
📁 Database (ตู้เก็บเอกสาร)
  ├── 📊 Table: users (ลิ้นชักเก็บข้อมูลผู้ใช้)
  │   ├── id, name, email (คอลัมน์)
  │   ├── 1, สมชาย, som@email.com (แถวที่ 1)
  │   └── 2, สมหญิง, ying@email.com (แถวที่ 2)
  │
  └── 📊 Table: todos (ลิ้นชักเก็บรายการงาน)
      ├── id, task, user_id (คอลัมน์)
      ├── 1, ซื้อของ, 1 (แถวที่ 1)
      └── 2, ทำงาน, 1 (แถวที่ 2)
```

**คำศัพท์:**
- **Database** = ตู้เก็บเอกสารทั้งชุด
- **Table** = ลิ้นชักแต่ละลิ้นชัก (เก็บข้อมูลแต่ละเรื่อง)
- **Column** = หัวข้อ (เช่น ชื่อ, อีเมล, เบอร์โทร)
- **Row** = แถว/ข้อมูลแต่ละรายการ
- **Primary Key** = รหัสประจำตัว (ไม่ซ้ำกัน)
- **Foreign Key** = อ้างอิงไปยัง table อื่น

---

## 📖 SQLite คืออะไร?

**SQLite** เป็น database แบบง่ายที่สุด:
- ไม่ต้อง install server
- ไม่ต้อง setup อะไร
- เก็บเป็นไฟล์เดียว (.db)
- เหมาะกับการเรียนรู้และ app ขนาดเล็ก
- ใช้ใน mobile apps, browsers, desktop apps

**เหมาะกับ:**
- 📱 Mobile apps
- 🖥️ Desktop applications
- 🎓 การเรียนรู้และทดลอง
- 📊 ข้อมูลขนาดเล็ก-ปานกลาง

---

## 📝 SQL พื้นฐาน 4 คำสั่ง

### 1. CREATE - สร้าง table

```sql
-- สร้าง table เก็บรายการ todo
CREATE TABLE todos (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  task TEXT NOT NULL,
  done BOOLEAN DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**อธิบาย:**
- `id` = รหัส (เพิ่มอัตโนมัติ)
- `task` = งานที่ต้องทำ (ต้องมีค่า)
- `done` = เสร็จหรือยัง (default = ยังไม่เสร็จ)
- `created_at` = วันที่สร้าง (default = เวลาปัจจุบัน)

### 2. INSERT - เพิ่มข้อมูล

```sql
-- เพิ่มงานใหม่
INSERT INTO todos (task) VALUES ('ซื้อของ');
INSERT INTO todos (task) VALUES ('ทำการบ้าน');
INSERT INTO todos (task, done) VALUES ('ออกกำลังกาย', 1);
```

### 3. SELECT - ดึงข้อมูล

```sql
-- ดึงทั้งหมด
SELECT * FROM todos;

-- ดึงเฉพาะงานที่ยังไม่เสร็จ
SELECT * FROM todos WHERE done = 0;

-- ดึงเฉพาะ task และ done
SELECT task, done FROM todos;

-- เรียงตามวันที่
SELECT * FROM todos ORDER BY created_at DESC;
```

### 4. UPDATE - แก้ไขข้อมูล

```sql
-- ทำเครื่องหมายว่าเสร็จแล้ว
UPDATE todos SET done = 1 WHERE id = 1;

-- แก้ไขข้อความ
UPDATE todos SET task = 'ซื้อของที่ตลาด' WHERE id = 1;
```

### 5. DELETE - ลบข้อมูล

```sql
-- ลบ todo ที่ id = 1
DELETE FROM todos WHERE id = 1;

-- ลบงานที่เสร็จแล้วทั้งหมด
DELETE FROM todos WHERE done = 1;
```

---

## 🔑 Primary Key และ Foreign Key

### Primary Key (รหัสประจำตัว)

```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY,  -- รหัสประจำตัว ไม่ซ้ำ
  name TEXT NOT NULL
);
```

**กฎ:**
- ต้องไม่ซ้ำกัน (UNIQUE)
- ต้องไม่เป็น NULL
- แต่ละ table มี 1 primary key

### Foreign Key (อ้างอิง)

```sql
-- Table ผู้ใช้
CREATE TABLE users (
  id INTEGER PRIMARY KEY,
  name TEXT
);

-- Table งาน (ของผู้ใช้คนไหน)
CREATE TABLE todos (
  id INTEGER PRIMARY KEY,
  task TEXT,
  user_id INTEGER,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

**ตัวอย่างข้อมูล:**

```
users:
id | name
1  | สมชาย
2  | สมหญิง

todos:
id | task        | user_id
1  | ซื้อของ      | 1        (งานของสมชาย)
2  | ทำการบ้าน   | 1        (งานของสมชาย)
3  | ทำงาน       | 2        (งานของสมหญิง)
```

---

## 🔍 การ JOIN ข้อมูล

ดึงข้อมูลจาก 2 tables พร้อมกัน:

```sql
-- ดึง todo พร้อมชื่อเจ้าของ
SELECT 
  todos.task,
  users.name
FROM todos
JOIN users ON todos.user_id = users.id;
```

**ผลลัพธ์:**
```
task        | name
ซื้อของ      | สมชาย
ทำการบ้าน   | สมชาย
ทำงาน       | สมหญิง
```

---

## 📝 Workshop Structure

Workshop นี้มี 2 ระดับ:

### Level 1: Todo App (ง่าย)
- 1 table เท่านั้น
- CRUD พื้นฐาน
- เหมาะสำหรับเริ่มต้น

### Level 2: Library System (ค่อนข้างง่าย)
- 3 tables (books, members, borrowings)
- relationships พื้นฐาน
- ฝึกการ JOIN

---

## 🛠️ เครื่องมือที่ต้องใช้

### 1. Node.js Package
```bash
npm install better-sqlite3
```

### 2. SQLite Browser (Optional - แนะนำ)
- Download: https://sqlitebrowser.org/
- ใช้ดู database ผ่าน GUI
- ทดสอบ queries ก่อนเขียนใน code

---

## 📖 เนื้อหา Workshop

- [Level 1: Guided Workshop](./level-1-guided/README.md) - Todo App Database (ง่าย)
- [Level 2: Challenge Workshop](./level-2-challenge/README.md) - Library System (ค่อนข้างง่าย)

---

## 💡 Tips สำหรับผู้เริ่มต้น

1. **เริ่มจากเข้าใจ concept** ก่อนเขียน code
2. **ใช้ DB Browser** ดูข้อมูลจะเข้าใจง่ายขึ้น
3. **ทดสอบ SQL** ใน browser ก่อนใส่ใน code
4. **เริ่มจาก table เดียว** แล้วค่อยเพิ่ม
5. **อย่ากลัวผิด** - ลบและสร้างใหม่ได้เสมอ

---

## 🎯 สิ่งที่จะได้เรียนรู้

✅ Database คืออะไรและทำไมต้องใช้  
✅ SQL 4 คำสั่งพื้นฐาน (CREATE, INSERT, SELECT, UPDATE, DELETE)  
✅ Primary Key และ Foreign Key  
✅ การ JOIN ข้อมูลพื้นฐาน  
✅ เชื่อมต่อ SQLite กับ Node.js  
✅ สร้าง app ที่เก็บข้อมูลถาวร  

---

**เริ่มเลย!** → [Level 1: Todo App Database](./level-1-guided/README.md)

**พร้อมหรือยัง? มาเริ่มสร้าง database กันเถอะ! 🚀**
