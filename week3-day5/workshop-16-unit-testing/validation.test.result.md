# 🔍 วิเคราะห์ Error และ Jest ทำงานอย่างไร

---

## 🐛 เกิดอะไรขึ้น?

### ปัญหาที่ 1: Error Message ไม่ตรงกัน

**Test คาดหวัง:**
```javascript
test('should reject empty task', () => {
  expect(() => validateTask('')).toThrow('Task cannot be empty');
  //                                      ^^^^^^^^^^^^^^^^^^^^^^^^
  //                                      คาดหวังข้อความนี้
});
```

**แต่โค้ดจริงใน `validation.js` throw:**
```javascript
function validateTask(task) {
  if (!task) {
    throw new Error('Task is required');  // ← โค้ดจริง throw ข้อความนี้
    //               ^^^^^^^^^^^^^^^^^^
  }
}
```

**ผลลัพธ์:**
```
❌ FAIL

Expected: "Task cannot be empty"
Received: "Task is required"
```

---

### ปัญหาที่ 2: Array ไม่มีข้อความที่คาดหวัง

**Test คาดหวัง:**
```javascript
test('should collect all validation errors', () => {
  const result = validateTodo({ task: '', ... });
  expect(result.errors).toContain('Task cannot be empty');
  //                               ^^^^^^^^^^^^^^^^^^^^^^^^
});
```

**แต่ array จริง ๆ มี:**
```javascript
["Task is required", "Priority must be...", "Invalid date format"]
//  ^^^^^^^^^^^^^^^^
//  มีข้อความนี้แทน
```

**ผลลัพธ์:**
```
❌ FAIL

Expected value: "Task cannot be empty"
Received array: ["Task is required", ...]
```

---

## 💡 ทำไมถึงเกิด?

นี่เป็นสถานการณ์ที่เจอบ่อยในการเขียน test:

```
┌─────────────────────────────────────────┐
│  สาเหตุที่โค้ดกับ Test ไม่ตรงกัน               │
└─────────────────────────────────────────┘

1. เขียนโค้ดก่อน → เขียน test ทีหลัง
   └─ อาจจำ error message ไม่ได้แม่นยำ

2. Copy-paste test จาก workshop
   └─ แต่แก้โค้ดเป็นข้อความอื่น

3. สองคนทำงานต่างส่วน
   └─ คนเขียนโค้ดใช้ข้อความหนึ่ง
   └─ คนเขียน test คาดหวังข้อความอื่น
```

---

## 🎯 นี่คือประโยชน์ของ Jest!

### Jest ช่วยจับความไม่ตรงกันทันที

```
┌─────────────────────────────────────────┐
│   ถ้าไม่มี Jest (Test ด้วยมือ)               │
└─────────────────────────────────────────┘

Developer:
"เอ... function นี้ throw error message อะไรนะ?
ต้องไปเปิดโค้ดดู... 
หรือว่าลองรันดู...
เสียเวลา..."

┌─────────────────────────────────────────┐
│   มี Jest                                │
└─────────────────────────────────────────┘

Jest:
"❌ FAIL! ไม่ตรงกัน!
Expected: 'Task cannot be empty'
Received: 'Task is required'

→ รู้ทันทีว่าไม่ตรงกัน
→ แก้ได้ทันที
→ ประหยัดเวลา!"
```

---

## 🔧 วิธีแก้ไข (2 วิธี)

### วิธีที่ 1: แก้ Test ให้ตรงกับโค้ด (แนะนำ)

**แก้ test ใน `validation.test.js`:**

```javascript
// ❌ เดิม
test('should reject empty task', () => {
  expect(() => validateTask('')).toThrow('Task cannot be empty');
});

// ✅ แก้เป็น (ตรงกับโค้ด)
test('should reject empty task', () => {
  expect(() => validateTask('')).toThrow('Task is required');
});
```

```javascript
// ❌ เดิม
test('should collect all validation errors', () => {
  // ...
  expect(result.errors).toContain('Task cannot be empty');
});

// ✅ แก้เป็น
test('should collect all validation errors', () => {
  // ...
  expect(result.errors).toContain('Task is required');
});
```

---

### วิธีที่ 2: แก้โค้ดให้ตรงกับ Test

**แก้โค้ดใน `validation.js`:**

```javascript
// ❌ เดิม
function validateTask(task) {
  if (!task) {
    throw new Error('Task is required');
  }
  
  if (typeof task !== 'string') {
    throw new Error('Task must be a string');
  }
  
  if (task.trim().length === 0) {
    throw new Error('Task cannot be empty');  // ← เดิมอยู่ที่นี่
  }
  // ...
}

// ✅ แก้เป็น (รวม 2 checks แรกเข้าด้วยกัน)
function validateTask(task) {
  if (!task || task.trim().length === 0) {
    throw new Error('Task cannot be empty');  // ← ใช้ message นี้
  }
  
  if (typeof task !== 'string') {
    throw new Error('Task must be a string');
  }
  // ...
}
```

---

## 📚 บทเรียนที่ได้

### 1. Tests = Living Documentation

```
Test เป็นเอกสารที่ "มีชีวิต":

test('should reject empty task', () => {
  expect(() => validateTask('')).toThrow('Task cannot be empty');
});

→ อ่านแล้วรู้ทันทีว่า:
   ✅ ถ้าส่ง empty string → ต้อง throw error
   ✅ Error message = "Task cannot be empty"

→ ไม่ต้องไปค้นโค้ดทั้งไฟล์!
```

### 2. Tests จับ Breaking Changes

```
สถานการณ์:
1. โค้ดเดิมใช้ message "Task is required"
2. คนอื่นแก้เป็น "Task cannot be empty"
3. Tests จะ FAIL ทันที ❌

→ รู้ทันทีว่ามีคนแก้
→ ตัดสินใจได้ว่า:
   ✅ เจตนาแก้ → อัพเดท test
   ❌ แก้พลาด → revert โค้ด
```

### 3. Consistency is Key

```javascript
// ❌ แย่ - ใช้ message ไม่สม่ำเสมอ
function validate(data) {
  if (!data.name) throw new Error('Name is required');
  if (!data.email) throw new Error('email required');  // lowercase
  if (!data.age) throw new Error('AGE REQUIRED!!!');   // uppercase + !!!
}

// ✅ ดี - ใช้ message สม่ำเสมอ
function validate(data) {
  if (!data.name) throw new Error('Name is required');
  if (!data.email) throw new Error('Email is required');  // consistent
  if (!data.age) throw new Error('Age is required');      // consistent
}
```

---

## 🎓 ทำความเข้าใจ Jest Error Messages

### 1. Structure ของ Error Message

```
● Validation Utils › validateTask › should reject empty task
  │        │              │                    │
  │        │              │                    └─ Test name
  │        │              └─ Nested describe (ถ้ามี)
  │        └─ describe name
  └─ Test suite indicator

expect(received).toThrow(expected)
       │                  │
       │                  └─ สิ่งที่ test คาดหวัง
       └─ สิ่งที่ได้จริง

Expected substring: "Task cannot be empty"  ← คาดหวัง
Received message:   "Task is required"      ← ได้จริง
```

### 2. Stack Trace

```
at validateTask (src/utils/validation.js:8:11)
   │              │                        │ │
   │              │                        │ └─ column 11
   │              │                        └─ line 8
   │              └─ file path
   └─ function name

→ บอกตำแหน่งที่เกิด error แม่นยำ
```

### 3. Code Context

```
   6 | function validateTask(task) {
   7 |   if (!task) {
>  8 |     throw new Error('Task is required');
      |           ^
   9 |   }

→ แสดงโค้ดบริบท
→ ชี้ตำแหน่งที่เกิด error (^)
```

---

## ✅ สรุป

### เกิดอะไรขึ้น?
```
โค้ดใช้ error message: "Task is required"
Test คาดหวัง:          "Task cannot be empty"

→ ไม่ตรงกัน → Test FAIL
```

### Jest ทำงานถูกหรือผิด?
```
✅ Jest ทำงาน 100% ถูกต้อง!

Jest ทำหน้าที่:
1. รัน test ทุกตัว
2. เปรียบเทียบ expected vs actual
3. ถ้าไม่ตรง → FAIL และบอก error ชัดเจน
4. แสดง error message ละเอียด

→ Jest เป็น "ผู้ตรวจสอบ" ที่ดี
→ ไม่มี bias, ไม่พลาด
→ บอกความจริงอย่างตรงไปตรงมา
```

### ควรทำอย่างไร?
```
Option 1: แก้ test ให้ตรงโค้ด (เร็วกว่า)
Option 2: แก้โค้ดให้ตรงกับที่ต้องการ

→ เลือกตามบริบท:
  - ถ้าโค้ดถูกต้อง → แก้ test
  - ถ้า test ถูกต้อง → แก้โค้ด
```

---

## 💡 Next Steps

**1. แก้ Tests ทันที:**
```bash
# แก้ไขใน validation.test.js
# เปลี่ยน "Task cannot be empty" → "Task is required"

# รัน test อีกครั้ง
npm test validation.test.js
```

**2. ควรได้:**
```
✅ PASS tests/unit/validation.test.js
  Validation Utils
    ✓ should reject empty task
    ✓ should collect all validation errors
    ...

Tests: 29 passed, 29 total ✨
```

**3. Commit:**
```bash
git add tests/unit/validation.test.js
git commit -m "test: fix error message expectations"
```

---
